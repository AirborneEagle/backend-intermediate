type User{
    firstName: String!
    lastName: String!
    email: String!
}
input UserInput{
    firstName: String!
    lastName: String!
    email: String!
    password: String!
}
type Query {
    getUser(id: String) : User
}
type Mutation {
  createUser(input: UserInput!): User
}